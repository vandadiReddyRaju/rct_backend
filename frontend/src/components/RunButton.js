// filepath: frontend/src/components/RunButton.js
import React from 'react';

const RunButton = ({ onClick }) => {
    return (
        <button onClick={onClick}>Run</button>
    );
};

export default RunButton;